<h1>All Accounts</h1>


<% if @users.length > 0 %>
  <ul>
    <% @users.each do |user| %>
      <li class="user"> <%= link_to user.username, user_path(user.id) %> </li>
    <% end %>
  </ul>
<% else %>
  <p> Nothing here. </p>
<% end %>
